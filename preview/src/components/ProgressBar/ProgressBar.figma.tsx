import { ProgressBar } from './ProgressBar';

export const meta = {
  component: ProgressBar,
  figmaNode: '',
  props: {
    label: 'Storage',
    value: 72,
    max: 100,
    color: 'blue',
  },
};
