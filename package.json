{
  "name": "figma-component-dsl",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/dsl-core",
    "packages/compiler",
    "packages/capturer",
    "packages/comparator",
    "packages/exporter",
    "packages/plugin",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.0-beta",
    "vitest": "^3.1.0"
  }
}
