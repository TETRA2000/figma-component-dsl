# @figma-dsl/renderer Documentation
> Generated by MAGI Documentation System
> Consensus rounds: 1 (no validation needed — zero contradictions) | Date: 2026-03-14
> Overall confidence: 0.94

## Table of Contents

1. [Overview](#overview)
2. [Architecture & Module Structure](#architecture--module-structure)
3. [Public API](#public-api)
4. [Rendering Pipeline](#rendering-pipeline)
5. [Fill & Color Rendering](#fill--color-rendering)
6. [Shape Rendering](#shape-rendering)
7. [Text Rendering](#text-rendering)
8. [Transform & Coordinate System](#transform--coordinate-system)
9. [Clipping & Visibility](#clipping--visibility)
10. [Stroke Rendering](#stroke-rendering)
11. [Font System](#font-system)
12. [Canvas Backend](#canvas-backend)
13. [Error Handling & Validation](#error-handling--validation)
14. [Integration Patterns](#integration-patterns)
15. [Performance Characteristics](#performance-characteristics)
16. [Build & Test](#build--test)
17. [Known Limitations](#known-limitations)
18. [Appendix: Documentation Confidence Report](#appendix-documentation-confidence-report)

---

## Overview
**Confidence**: 0.98 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

The `@figma-dsl/renderer` package is a PNG rasterization engine that converts compiled `FigmaNodeDict` trees (produced by `@figma-dsl/compiler`) into PNG images using server-side canvas rendering via `@napi-rs/canvas` (Skia backend). It enables headless rendering without requiring Figma or a browser environment.

The package fits into the DSL pipeline as: `DSL → Compile → **Render** → Compare → Export → Figma`.

Key characteristics:
- Single-file implementation (~440 lines)
- Stateless design (no mutable global state beyond one-time font registration and canvas pool)
- O(n) linear tree traversal
- Supports 9 node types, solid/linear/radial gradient fills, multi-stroke rendering with alignment (INSIDE/CENTER/OUTSIDE), text with decorations and CJK support, clipping, and canvas pooling

---

## Architecture & Module Structure
**Confidence**: 0.99 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

```
packages/renderer/
├── src/
│   ├── index.ts              # Barrel export (re-exports renderer.ts + image-loader.ts)
│   ├── renderer.ts           # Core rendering implementation (~440 lines)
│   ├── image-loader.ts       # Image source collection, resolution, and preloading
│   ├── renderer.test.ts      # Renderer test suite (208 lines)
│   └── image-loader.test.ts  # Image loader test suite (18 test statements)
├── dist/                     # Compiled output (ESM + types)
├── package.json
└── tsconfig.json
```

**Internal organization** of `renderer.ts`:

| Section | Purpose |
|---------|---------|
| Imports & type definitions | `RgbaColor`, `RenderOptions`, `RenderResult`, `RenderError` |
| Default configuration | `DEFAULT_OPTIONS` (white bg, scale 1) |
| Global state | `fontsRegistered` flag, canvas pool |
| Font initialization | `initializeRenderer()` — registers Inter + Noto Sans JP |
| CJK detection | `containsCJK()`, `chooseFontFamily()` |
| Color utility | `rgbaToString()` |
| Font weight utility | `fontWeightToCss()` |
| Fill rendering | `applyFills()` — solid + linear gradient + radial gradient |
| Corner radius resolution | `resolveCornerRadius()`, `hasCornerRadius()` |
| Shape utility | `drawRoundedRect()` |
| Node rendering | `renderNode()` — recursive tree traversal with effects and blend modes |
| Stroke rendering | Multi-stroke loop with INSIDE/CENTER/OUTSIDE alignment |
| Text rendering | `renderText()` with word-wrap, text transform, text stroke, text shadow, gradient fills |
| Text decoration | `drawTextDecoration()` — UNDERLINE/STRIKETHROUGH lines |
| Text transform | `applyTextTransform()` — UPPERCASE/LOWERCASE/CAPITALIZE |
| Canvas pooling | `acquireCanvas()`, `releaseCanvas()` |
| Debug overlay | `renderDebugOverlay()` — frame borders, padding, computed sizes |
| Public API | `render()`, `renderToFile()` |

**Additional modules**:

| Module | Purpose |
|--------|---------|
| `effects.ts` | Drop shadow, layer blur, blend mode rendering via canvas API |
| `font-manager.ts` | Bundled and custom font registration, CJK font resolution |

**Evidence**: `src/index.ts:1`, `src/renderer.ts`, `package.json`

---

## Public API
**Confidence**: 0.99 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Types

```typescript
interface RgbaColor {
  r: number;    // 0–1 float
  g: number;
  b: number;
  a: number;
}

interface RenderOptions {
  backgroundColor: RgbaColor;   // Default: white (1,1,1,1)
  scale: number;                // Default: 1
  debugLayout?: boolean;        // Overlay layout debug info (frame borders, padding, sizes)
  assetDir?: string;            // Base directory for image path resolution
  imageCache?: ImageCache;      // Pre-loaded image cache (from preloadImages())
}

type ImageCache = ReadonlyMap<string, Image>;


interface RenderResult {
  pngBuffer: Buffer;    // PNG-encoded image bytes
  width: number;        // Final canvas width (post-scale)
  height: number;       // Final canvas height (post-scale)
}
```

### RenderError Class

```typescript
class RenderError extends Error {
  readonly nodePath: string;   // Tree path to failing node
  readonly nodeType: string;   // Figma node type
}
```

### Functions

#### `initializeRenderer(fontDir: string): void`
Registers all `.otf` and `.ttf` fonts from the given directory with `@napi-rs/canvas`. Idempotent — subsequent calls are no-ops via internal `fontsRegistered` guard. **Must be called before any `render()` calls.**

#### `render(node: FigmaNodeDict, options?: Partial<RenderOptions>): RenderResult`
Core rendering function. Creates canvas at node dimensions × scale, renders the tree recursively, and returns PNG buffer with final dimensions. Throws `RenderError` if canvas dimensions are ≤ 0.

#### `renderToFile(node: FigmaNodeDict, outputPath: string, options?: Partial<RenderOptions>): RenderResult`
Convenience wrapper: calls `render()` → writes PNG to disk via `writeFileSync()` → returns same `RenderResult`.

### Image Loading Functions

#### `collectImageSources(node: FigmaNodeDict): Set<string>`
Traverses a compiled node tree and collects all image source references from IMAGE nodes (`imageSrc`) and IMAGE fills (`fillPaints` with `type: 'IMAGE'`). Returns deduplicated `Set<string>`.

#### `resolveImageSource(src: string, assetDir: string): string`
Resolves a relative image path against an asset directory. HTTP/HTTPS URLs and absolute paths pass through unchanged; relative paths are resolved via `resolve(assetDir, src)`.

#### `preloadImages(sources: Set<string>, assetDir: string): Promise<ImageCache>`
Loads all collected image sources in parallel using `Promise.allSettled()`. Failed loads are silently omitted (the renderer draws a placeholder instead). Returns an immutable `Map<string, Image>`.

**Evidence**: `src/renderer.ts:6-34,43-52,249-293`, `src/image-loader.ts:14-81`

---

## Rendering Pipeline
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

The `render()` function processes nodes through this pipeline:

```
FigmaNodeDict (compiled tree)
    │
    ├── 1. Compute dimensions: ceil(size.x × scale), ceil(size.y × scale)
    ├── 2. Validate dimensions > 0 (throws RenderError)
    ├── 3. Create @napi-rs/canvas at final dimensions
    ├── 4. Apply ctx.scale(scale, scale)
    ├── 5. Fill background color
    ├── 6. renderNode(ctx, root) — recursive depth-first traversal
    │       ├── Check visibility (skip if invisible)
    │       ├── Save canvas state
    │       ├── Apply node transform (absolute 2x3 affine matrix)
    │       ├── Apply clipping if clipContent
    │       ├── Render fills (solid/linear gradient/radial gradient)
    │       ├── Render all strokes (with INSIDE/CENTER/OUTSIDE alignment)
    │       ├── Reset transform to identity
    │       ├── Recurse into children
    │       ├── Render text decorations (UNDERLINE/STRIKETHROUGH)
    │       └── Restore canvas state
    ├── 7. canvas.toBuffer('image/png')
    └── 8. Return { pngBuffer, width, height }
```

**Evidence**: `src/renderer.ts:113-205,249-283`

---

## Fill & Color Rendering
**Confidence**: 0.94 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Solid Fills
- Iterates through `node.fillPaints` array
- Skips paints with `visible === false`
- Compounds opacity: `globalAlpha = (node.opacity ?? 1) × paint.opacity`
- Converts RGBA (0–1 normalized) to CSS `rgba()` string via `rgbaToString()`

### Linear Gradient Fills
- `GRADIENT_LINEAR` type
- Transforms `gradientTransform` matrix from UV space (0,0)→(1,1) to canvas coordinates
- If `gradientTransform` is undefined, defaults to horizontal gradient
- Color stops from `gradientStops` array applied via `gradient.addColorStop()`

### Image Fills
- `IMAGE` type
- Renders image from `imageCache` using `drawImageOnNode()` with scale mode support via `computeDrawInstruction()` from `@figma-dsl/core`
- Supports four scale modes: FILL (cover, may crop), FIT (contain, may letterbox), CROP (center crop), TILE (repeat pattern via `createPattern()`)
- Falls back to `drawImagePlaceholder()` if image not found in cache — gray background with crosshatch pattern

### Radial Gradient Fills
- `GRADIENT_RADIAL` type
- Center and radius computed from node dimensions using normalized 0–1 coordinates
- Default center: `(width/2, height/2)`, default radius: `max(width, height) / 2`
- Uses `ctx.createRadialGradient(cx, cy, 0, cx, cy, radius)` for circular spread

### Color Conversion
- Input: `RgbaColor { r, g, b, a }` with values 0–1
- Output: CSS `rgba(round(r×255), round(g×255), round(b×255), alpha)`

### Design Pattern
`applyFills()` uses a callback pattern — `drawShape` receives the context, allowing the same fill logic to apply to rectangles, rounded rectangles, and ellipses without duplication.

**Evidence**: `src/renderer.ts:54-98`

---

## Shape Rendering
**Confidence**: 0.96 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Node Type Dispatch

| Type | Rendering Behavior |
|------|-------------------|
| FRAME, COMPONENT, COMPONENT_SET, INSTANCE | Rectangular shape with fills/strokes |
| RECTANGLE | Rectangle with optional corner radius |
| ROUNDED_RECTANGLE | Rectangle with corner radius |
| ELLIPSE | Ellipse via `ctx.ellipse()` (center, radiusX, radiusY) |
| IMAGE | Image from cache with cornerRadius clipping; placeholder if not cached |
| TEXT | Delegated to `renderText()` |
| GROUP | Transparent container — children rendered, no shape |

### Rounded Rectangles
- Uses `ctx.roundRect()` when corner radius is present
- Supports both uniform `cornerRadius` (single number) and per-corner `cornerRadii` (array `[topLeft, topRight, bottomRight, bottomLeft]`)
- `resolveCornerRadius()` helper normalizes both formats for the canvas API
- Falls back to `ctx.rect()` for zero or undefined corner radius
- Same logic used for fill shapes, stroke shapes, and clipping regions

### Ellipses
- Rendered as arc with center at `(w/2, h/2)`, radii `(w/2, h/2)`, full 2π sweep

**Evidence**: `src/renderer.ts:100-111,136-174`

---

## Text Rendering
**Confidence**: 0.92 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

Text rendering depends on **pre-computed baseline data** from the compiler's text measurer:

### Prerequisites
- `node.textData` must provide `characters` and `lines` array
- `node.derivedTextData` must provide `baselines` and `fontMetaData`
- **Silent failure**: Returns without rendering if either is missing

### Process
1. **Font setup**: Constructs CSS font string `"${weight} ${fontSize}px ${family}"`
   - Font size defaults to 14px, family to 'Inter', weight to 400
   - Weight extracted from `derivedTextData.fontMetaData[0].fontWeight`
2. **Color**: Uses first fill paint's color, defaults to black
3. **Word-wrap detection**: If `node.textAutoResize === 'HEIGHT'`, the renderer word-wraps text at `node.size.x` width using `canvas.measureText()` for word-boundary detection. Each wrapped line is drawn at the computed line height offset.
4. **Per-line rendering** (non-wrapped path): Iterates `textData.lines`:
   - Baseline Y position from `derivedTextData.baselines[i].lineY`
   - Horizontal alignment via `canvas.measureText()`:
     - LEFT: offset = 0
     - CENTER: offset = (nodeWidth − lineWidth) / 2
     - RIGHT: offset = nodeWidth − lineWidth
   - Renders via `ctx.fillText(line, xOffset, baselineY)`

### Design Rationale
Baseline-driven rendering avoids text measurement in the render hot path. The compiler pre-computes all positioning, making the renderer stateless with respect to font metrics. The `textAutoResize: 'HEIGHT'` path re-measures at render time to produce accurate word-wrapped output matching the Figma plugin behavior.

**Evidence**: `src/renderer.ts:207-247`

---

## Transform & Coordinate System
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Absolute Transform Model
Each node's transform is **absolute** (root-relative), pre-computed by the compiler. The renderer does not compose parent–child transforms.

```typescript
const [[a, c, tx], [b, d, ty]] = node.transform;
ctx.setTransform(a, b, c, d, tx, ty);
// ... render node shape ...
ctx.setTransform(1, 0, 0, 1, 0, 0);  // Reset to identity before children
```

### State Isolation
- `ctx.save()` at node entry, `ctx.restore()` at node exit
- Transform reset to identity before recursing into children (children have their own absolute transforms)
- Clipping paths scoped to the save/restore block

**Evidence**: `src/renderer.ts:116-120,196-197,204`

---

## Clipping & Visibility
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Visibility
- `node.visible === false` causes immediate return (no rendering, no children)
- Per-paint visibility: `paint.visible === false` skips individual paints

### Clipping
- Activated when `node.clipContent === true`
- Creates clipping region (rounded rect if cornerRadius, else rectangle) before rendering shapes
- Affects the node itself and all descendants until `ctx.restore()`

**Evidence**: `src/renderer.ts:114,126-134,68`

---

## Stroke Rendering
**Confidence**: 0.95 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

- **All strokes** in `node.strokes` are rendered in order (layered)
- Stroke weight: `node.strokeWeight ?? stroke.weight`
- Stroke color: RGBA converted to CSS string
- Opacity: Combined with node opacity via `globalAlpha`
- Shape-specific rendering:
  - ELLIPSE: `ctx.ellipse()` outline
  - Rounded rect: `ctx.roundRect()` stroke
  - Default: `ctx.strokeRect()`

### Stroke Alignment
- **CENTER** (default): Standard canvas stroke centered on the shape edge
- **INSIDE**: Uses `ctx.save()` + clip to node shape + 2× stroke weight (half inside, half clipped away) + `ctx.restore()`
- **OUTSIDE**: Uses `ctx.save()` + inverse clip (full canvas minus node shape, via `evenodd` fill rule) + 2× stroke weight + `ctx.restore()`

### Limitations
- No support for dashed/dotted strokes

**Evidence**: `src/renderer.ts:176-194`

---

## Font System
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Initialization
```typescript
let fontsRegistered = false;

export function initializeRenderer(fontDir: string): void {
  if (fontsRegistered) return;
  // ... register all .otf/.ttf files as 'Inter' ...
  fontsRegistered = true;
}
```

### Registered Fonts
From `@figma-dsl/core/fonts`:

**Inter** (Latin script):
- Inter-Regular.otf (400)
- Inter-Medium.otf (500)
- Inter-SemiBold.otf (600)
- Inter-Bold.otf (700)

**Noto Sans JP** (CJK/Japanese script):
- NotoSansJP-Regular.ttf (400)
- NotoSansJP-Bold.ttf (700)

### CJK Font Selection
The renderer auto-detects CJK characters (U+3000–U+9FFF, U+F900–U+FAFF) in text content via `containsCJK()`. When CJK characters are present, `chooseFontFamily()` selects `'Noto Sans JP'` instead of `'Inter'`. This is transparent to DSL authors — no `fontFamily` override needed.

**Evidence**: `src/renderer.ts:41-52`

---

## Canvas Backend
**Confidence**: 0.98 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

The renderer uses **@napi-rs/canvas** (Rust/NAPI-RS wrapper around Skia):

**Advantages**:
- Server-side rendering (no browser required)
- High-quality antialiasing via Skia
- Deterministic output for visual regression testing
- Native PNG encoding performance

**Canvas API surface used**:
- `createCanvas(width, height)` — Canvas creation
- `ctx.getContext('2d')` — 2D rendering context
- `GlobalFonts.registerFromPath()` — Font registration
- Drawing: `fillRect`, `strokeRect`, `roundRect`, `ellipse`, `fillText`, `measureText`
- State: `save`, `restore`, `setTransform`, `scale`, `clip`
- Paint: `fillStyle`, `strokeStyle`, `lineWidth`, `globalAlpha`, `createLinearGradient`, `createRadialGradient`
- Output: `canvas.toBuffer('image/png')` — PNG encoding

**Evidence**: `src/renderer.ts:1,265-281`, `package.json:20`

---

## Error Handling & Validation
**Confidence**: 0.91 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### RenderError
Custom error class with `nodePath` (tree path string) and `nodeType` (Figma node type) properties for diagnostic context.

### Validated
- Canvas dimensions must be > 0 after scaling — throws `RenderError` if invalid

### Not Validated
- Transform matrix format (assumed valid from compiler)
- RGBA value ranges (assumed 0–1)
- Scale factor (no bounds checking — extreme values could allocate excessive memory)
- Text data completeness (silent failure if `textData` or `derivedTextData` missing)
- Array access safety (non-null assertions on `strokes[0]!`, `fillPaints[0]!`)
- Node tree structure (children assumed non-null arrays)

**Evidence**: `src/renderer.ts:25-34,257-262`

---

## Integration Patterns
**Confidence**: 0.96 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### CLI Integration

The renderer is used in multiple CLI commands:

1. **`figma-dsl render`**: Compile DSL → render to PNG file
2. **`figma-dsl pipeline`**: Compile → Render → Capture → Compare pipeline
3. **`figma-dsl batch`**: Batch-process multiple DSL files to PNG

### Service Initialization
```typescript
// CLI initServices() pattern
initializeRenderer(fontDir);
textMeasurer.initialize(fontDir);
```
Font directory resolution searches: CLI dist directory → cwd `packages/dsl-core/fonts` → `node_modules/@figma-dsl/core/fonts`.

### Data Flow
```
@figma-dsl/core (DSL definitions)
    ↓
@figma-dsl/compiler (compileWithLayout)
    ↓
FigmaNodeDict (compiled tree)
    ↓
@figma-dsl/renderer (render → PNG buffer)
    ↓
@figma-dsl/comparator (compare → similarity score)
```

The renderer is a **terminal module** — it produces PNG output and does not feed data to other packages within the rendering path.

**Evidence**: CLI `src/cli.ts:8,34-38,96-132`, `src/batch-processor.ts:6,59-99`

---

## Performance Characteristics
**Confidence**: 0.88 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Complexity
- **Tree traversal**: O(n) where n = total nodes
- **Per-node rendering**: O(1) constant time
- **Fill rendering**: O(f) where f = fills per node (typically 1–2)
- **Text rendering**: O(l) where l = lines per text node

### Memory Usage
- **Canvas buffer**: width × height × 4 bytes (RGBA), stored in native memory
- **PNG buffer**: Compressed, typically 10–50% of raw canvas size
- **Call stack**: O(depth) for recursive traversal

### Characteristics
- All operations are **synchronous** — blocks the event loop
- **Canvas pooling**: `acquireCanvas(w, h)` / `releaseCanvas(canvas, w, h)` reuse canvases by `${width}x${height}` key, reducing allocation in batch operations
- Font registration is one-time per process (idempotent)
- Scaling produces proportionally larger canvas (2× scale = 4× pixels)
- PNG encoding via native Rust/Skia (fast)

**Evidence**: `src/renderer.ts:265,281`

---

## Build & Test
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

### Commands
```bash
npm run build    # tsc
npm run test     # vitest run
```

### TypeScript Configuration
- Target: ES2023, Module: Node16 (ESM)
- Strict mode + `noUncheckedIndexedAccess`
- Composite build with project references to `dsl-core` and `compiler`
- Declarations and source maps generated

### Dependencies

| Package | Version | Purpose |
|---------|---------|---------|
| `@figma-dsl/core` | 0.1.0 | Base types |
| `@figma-dsl/compiler` | 0.1.0 | `FigmaNodeDict` type, compilation |
| `@napi-rs/canvas` | ^0.1.96 | Skia-based canvas rendering |

### Test Coverage (9 suites)

| Suite | Tests | Coverage |
|-------|-------|----------|
| Basic shapes | 6 | Solid fills, uniform corner radius, per-corner radii, ellipses, groups |
| Text | 3 | Simple text, multiline, center-aligned |
| Gradients | 2 | Linear gradient, gradient with angle |
| Strokes | 1 | Bordered rectangle |
| Visibility | 1 | Invisible nodes skipped |
| Scaling | 1 | 2× scale output dimensions |
| Layout integration | 1 | Auto-layout button with `compileWithLayout()` |
| File I/O | 1 | `renderToFile()` writes PNG to disk |
| Error handling | 1 | Zero-size canvas throws `RenderError` |

### Coverage Gaps
- No tests for complex transforms (rotation, skew), multi-paint fills, clipping paths, nested opacity
- No tests for text with missing baseline data, font fallback scenarios
- No tests for ellipse strokes, multiple strokes, stroke alignment
- No performance benchmarks

**Evidence**: `package.json:14-24`, `tsconfig.json`, `src/renderer.test.ts`

---

## Effects System (Banner Mode)
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Code review

The `effects.ts` module provides visual effects rendering for Banner Mode nodes:

### Functions

```typescript
function applyDropShadow(ctx: SKRSContext2D, effect: DropShadowEffect): void;
function clearShadow(ctx: SKRSContext2D): void;
function applyLayerBlur(
  ctx: SKRSContext2D, effect: LayerBlurEffect,
  renderFn: () => void,
  bounds: { x: number; y: number; width: number; height: number }
): void;
function applyBlendMode(ctx: SKRSContext2D, blendMode: BlendMode): void;
function resetBlendMode(ctx: SKRSContext2D): void;
function applyEffects(
  ctx: SKRSContext2D, effects?: EffectDefinition[], blendMode?: BlendMode
): () => void;  // Returns cleanup function
```

- **Drop shadow**: Sets `shadowColor`, `shadowBlur`, `shadowOffsetX`, `shadowOffsetY` before drawing. Applied before coordinate transforms to avoid the @napi-rs/canvas translate bug.
- **Layer blur**: Renders to off-screen canvas with `ctx.filter = 'blur(Xpx)'`, composites result back.
- **Blend mode**: Maps DSL blend modes to `globalCompositeOperation` values (MULTIPLY→multiply, SCREEN→screen, etc.).
- **State management**: All effects bracketed with `ctx.save()`/`ctx.restore()`.

> **Note:** `BACKGROUND_BLUR` is deferred to phase 2 (requires two-pass rendering).

---

## Font Manager (Banner Mode)
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Code review

The `font-manager.ts` module manages bundled and custom font registration:

### Functions

```typescript
function initializeFontManager(options: FontManagerOptions): string[];
function resolveFontFamily(text: string, requestedFamily?: string): string;
```

### FontManagerOptions

```typescript
interface FontManagerOptions {
  fontDir: string;                    // bundled fonts directory
  assetDir?: string;                  // base for relative font paths
  customFonts?: FontRegistration[];   // from DSL file's fonts export
}
```

- **Bundled fonts**: Inter (400–700) for Latin, Noto Sans JP (400, 700) for CJK
- **Custom fonts**: Registered from `FontRegistration[]`, supports `.ttf`, `.otf`, `.woff2`
- **WOFF2 decompression**: Uses `@woff2/woff2-rs` peer dependency; emits error if not installed
- **CJK auto-detection**: `resolveFontFamily()` returns `'Noto Sans JP'` when text contains CJK characters (U+3000–U+9FFF, U+F900–U+FAFF)
- **Buffer retention**: Font buffers stored in module-level Map to prevent GC

---

## Extended Text Rendering (Banner Mode)
**Confidence**: 0.97 | **Consensus**: Full | **Sources**: Code review

Banner Mode extends text rendering with:

- **Text transform**: `applyTextTransform()` converts text before measuring and rendering (UPPERCASE, LOWERCASE, CAPITALIZE)
- **Text stroke**: After `fillText()`, applies `strokeText()` with specified color and width
- **Text shadow**: Sets canvas shadow properties before `fillText()` for drop shadow effect
- **Gradient text**: Creates `CanvasGradient` matching text bounds when gradient fills are used
- **Text opacity**: Applied via `globalAlpha` for semi-transparent text layering

All enhancements are conditional — standard text rendering is unchanged when Banner properties are absent.

---

## Known Limitations
**Confidence**: 0.92 | **Consensus**: Full | **Sources**: Architect, Developer, Analyst

1. ~~**No shadow/blur effects**~~: **Resolved** — Banner Mode adds DROP_SHADOW and LAYER_BLUR effects via the effects helper.
2. **No dashed/dotted strokes**: Stroke dash patterns not supported.
3. ~~**Unused `assetDir` option**~~: **Resolved** — Image loading now uses `imageCache` (pre-loaded via `preloadImages()`) for rendering IMAGE nodes and fills.
4. **Silent text failure**: Text nodes without `textData`/`derivedTextData` render as blank without error.
5. **No input validation**: Beyond dimension checks, no validation of RGBA ranges, scale bounds, or transform matrices.
6. **Synchronous only**: All operations block the event loop; no async rendering support.
7. **CJK font limited to Japanese**: Only Noto Sans JP is bundled; Chinese (Simplified/Traditional) and Korean text may render with fallback glyphs.

### Resolved Limitations
- ~~**Single stroke only**~~: All strokes in `node.strokes[]` are now rendered in order.
- ~~**Linear gradients only**~~: `GRADIENT_RADIAL` is now supported via `ctx.createRadialGradient()`.
- ~~**Inter font only**~~: CJK text auto-detected and rendered using Noto Sans JP.
- ~~**Uniform corner radius**~~: Both uniform `cornerRadius` and per-corner `cornerRadii` are supported.
- ~~**Stroke alignment ignored**~~: INSIDE, CENTER, and OUTSIDE alignment now implemented via clipping.
- ~~**No canvas reuse**~~: Canvas pooling (`acquireCanvas`/`releaseCanvas`) now reuses canvases by dimension.

---

## Appendix: Documentation Confidence Report

### Confidence Summary

| Section | Confidence | Consensus Level |
|---------|-----------|----------------|
| Overview | 0.98 | Full (3/3) |
| Architecture | 0.99 | Full (3/3) |
| Public API | 0.99 | Full (3/3) |
| Rendering Pipeline | 0.97 | Full (3/3) |
| Fill & Color | 0.94 | Full (3/3) |
| Shape Rendering | 0.96 | Full (3/3) |
| Text Rendering | 0.92 | Full (3/3) |
| Transform & Coordinates | 0.97 | Full (3/3) |
| Clipping & Visibility | 0.97 | Full (3/3) |
| Stroke Rendering | 0.90 | Full (3/3) |
| Font System | 0.97 | Full (3/3) |
| Canvas Backend | 0.98 | Full (3/3) |
| Error Handling | 0.91 | Full (3/3) |
| Integration Patterns | 0.96 | Full (3/3) |
| Performance | 0.88 | Full (3/3) |
| Build & Test | 0.97 | Full (3/3) |
| Known Limitations | 0.92 | Full (3/3) |

### Unresolved Items
None. All three agents agreed on technical facts with zero contradictions.
